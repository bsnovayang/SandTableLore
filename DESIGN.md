# 沙盤傳說 SandTableLore — 中世紀卡牌戰棋 設計規劃

> 一句話定位：**Battleline 的三路推進盤面 + 爐石的資源／構築循環 + 世紀帝國的文明兵種特色。**

---

## 1. 核心迴圈

```
選文明 → 組牌(20張) → 對戰NPC → 勝負結算獲得寶石 → 抽卡包 → 擴充牌庫 → 回到組牌
```

- **寶石**：勝利 +40、失敗 +15。
- **卡包**：100 寶石／3 張隨機卡（含中立與各文明卡）。
- **牌組規則**：固定 20 張、同名卡最多 2 張、只能放「中立卡 + 自己文明的卡」、受收藏數量限制。

---

## 2. 盤面

- **3 路 × 6 格，縱向配置**：敵方城堡在上、我方城堡在下，三路是三個並排的直行，
  第 1 格貼近我方城堡、第 6 格貼近敵方城堡。單位往上推進。
- 單位只在自己的那一路前進；走到底（與城堡距離 ≤ 射程且該路前方沒有敵人）即**直接攻擊城堡**。
- **城堡血量 15**：一隻 4/x 的兵摸到 3~4 下就結束，所以「漏一路」是致命的，這是整個遊戲的張力來源。

### 領土（Battleline 的「佔線」轉化）
- 每路各自記錄雙方的**領土線**，開局各佔 1 格。
- **只能把單位放在自己領土內的空格**。領土往前鋪 → 出兵位置更前面 → 更快壓到城堡。
- 限制：`我方領土 + 敵方領土 ≤ 6`（不能穿過對方的領土）。

---

## 3. 資源（爐石式，但把「水晶自然成長」改成玩家抉擇）

每回合開始：**金幣回滿至上限 → 抽 1 張牌 → 執行「村民抉擇」（二選一，強制）**

| 選項 | 效果 | 戰略意義 |
|---|---|---|
| 🪙 開發 | 金幣上限 +1（上限 10） | 長期滾雪球，打後期大牌 |
| 🚩 拓荒 | 指定一路，領土 +1 格 | 立刻換取推進速度／壓制 |

這一個抉擇就是整局的節奏軸：**貪發育 vs 搶節奏**，等同爐石的「貪 vs 快攻」但由玩家每回合親手決定。

- 起始金幣上限 3、手牌上限 5（超過的抽牌燒掉）、牌庫抽完進入疲勞（每次抽牌城堡 -1）。
- 先手起手 3 張、後手起手 4 張。

---

## 4. 回合流程

1. 回合開始：金幣回滿、抽牌、**我方所有單位解除疲勞與召喚失調**
2. 村民抉擇（強制）
3. 打牌（部署單位／建築、施放法術）、使用英雄技能（每回合 1 次）
4. **手動操作每個單位**：點單位 → 點綠框格子移動 / 點紅框敵人或敵方城堡攻擊
5. 結束回合 → 建築荊棘結算 → 換手

### 行動與戰鬥規則
- **行動額度**：每個單位每回合可「**移動一次 + 攻擊一次**」，順序不限（可先打再走，也可只走不打）。
  棋子左上角標示**還剩什麼行動**：`移`=只剩移動、`攻`=只剩攻擊、`✓`=已用完、`💤`=召喚失調。
- **召喚失調**：當回合部署的單位不能行動（顯示 💤），對手因此有一回合的反應時間。
- **移動**：往前推進，格數依速度；被單位擋住就過不去。
- **換線／後退**：任何單位都能橫移到鄰路或往後撤（介面標黃框 ⇄），但**本回合無法再攻擊**。
  蒙古的「機動」單位不受此限 —— 換線或後退後仍可攻擊，這是它的核心賣點。
- **射程**：以格數計算，**跨到鄰路要多花 1 點射程**（每差一路算 2 格）。
  所以射程 2 的弓兵打得到鄰路正側面的敵人，而近戰（射程 1）依然只能打同一路。
- **攻擊**：同一路、前方、射程內的敵人。該路前方淨空且與城堡距離 ≤ 射程時可直擊城堡。
- **反擊**：近戰（射程 1）攻擊時**雙方同時扣血**；遠程單位攻擊不受反擊。
- **打帶跑**：有「機動」的單位在**移動之後**發動的近戰攻擊不受反擊 —— 這是蒙古的核心優勢。
- 傷害 = `攻擊力 − 防禦力`（最低 1）。
- **攻城**：攻城槌本身攻擊力只有 2，但對**城堡與建築**額外 +4 —— 專職拆遷，正面對砍很弱。
- **疲勞遞增**：牌庫抽完後，每次抽牌的傷害逐次 +1，確保對局一定會結束。

> 為什麼要反擊：沒有反擊的話，近戰完全沒有風險，「該不該換血」這個決策就消失了，
> 遠程單位的優勢也會被稀釋。加入反擊後，長矛兵 2/5 這種高血量單位才真正有守備價值。

> **為什麼跨路要多花射程**：一開始改成純曼哈頓距離（跨路只算 1 格），結果遠程單位的
> 覆蓋面積從 2 格暴增到 8 格且不受反擊 —— 一個弓兵就能守住三路，擋路的路障可以被繞著打，
> 速度也不再能閃避。實測英國衝到 **83%**、蒙古崩到 **16%**、條頓 31%，整個三路結構等於瓦解。
> 改成跨路計 2 格之後，「弓兵能打鄰排」的需求有滿足，分路的張力也保住了。

## 5. 文明（對應原始四文明）

| 文明 | 專屬特色 | 英雄技能（2 金） |
|---|---|---|
| 🏹 不列顛長弓帝國（獅心王理查） | **遠程壓制**：長弓手射程 2~3，敵人還沒走到就被射殺 | **齊射命令**：本回合所有遠程單位攻擊 +1 |
| 🐎 蒙古游牧汗國（成吉思汗） | **機動換線**：輕騎速度 2，被擋住時自動橫移到鄰路 | **疾風突襲**：指定一個騎兵立刻行動一次 |
| ⚜️ 法蘭西聖騎士（聖女貞德） | **盾牆推進**：高血量＋聖盾（免疫第一次傷害）；同路前後相連時**防禦力加倍** | **神聖庇護**：給最前線單位一個聖盾 |
| 🏰 條頓騎士團（巴巴羅薩） | **防守反擊**：箭塔／路障建築卡，擋路並對周圍敵人造成反彈傷害 | **加固城防**：建築回復 5 點，或城堡回復 2 點 |

### 關鍵字
`聖盾` 免疫第一次受到的傷害 ·　`陣型` 前後相連時防禦加倍 ·　`機動` 換線／後退後仍可攻擊，且移動後攻擊不受反擊 ·　`荊棘` 每回合對周圍敵人造成傷害 ·　`攻城` 對建築與城堡額外傷害

---

## 5.5 兵種相剋（全域規則，+2 傷害）

每個單位屬於**唯一一種兵種**，卡面右上角標示。兩個三角：

```
   兵種三角                     攻城三角
   🏹遠程 ──► 🗡步兵            🗡一般單位 ──► 🔨攻城
     ▲           │                ▲             │
     │           ▼                │             ▼
   🐎騎兵 ◄──────┘             🏰建築 ◄─────────┘
```

| 兵種 | 卡牌 |
|---|---|
| 🗡 步兵／槍 | 民兵、長矛兵、劍士、條頓騎士 |
| 🐎 騎兵 | 斥候、輕騎兵、聖騎士、重裝騎士、十字軍 |
| 🏹 遠程 | 弓箭手、長弓手、精銳長弓手、遊騎射手、箭塔 |
| 🔨 攻城 | 攻城槌 |
| 🏰 建築 | 路障、箭塔※ |

※箭塔同時是建築；`kind` 取 🏹遠程，`building` 關鍵字另外提供建築性質。

### 為什麼是全域規則，不是卡片效果
量測結果決定的：

| 做法 | 觸發率 |
|---|---|
| 卡片關鍵字（舊的「反騎 +2」） | **2.9%** |
| 全域三角 | **約 20%** |

20 張牌組裡，單張卡的期望登場次數只有 1～2 次 —— 把核心規則綁在卡片上，等於放進一個幾乎抽不到的容器。
玩家學了規則卻用不到，是最糟的複雜度：**付了記憶成本，沒拿到決策**。
全域規則的記憶成本反而更低：一個三角形取代 N 個關鍵字，而且是「看兵種圖示」而非「讀卡面小字」。

長矛兵原本的 `反騎 +2` 已併入「步兵剋騎兵」，避免 +2 疊 +2。

### 文明層級的相剋是刻意保留的
因為各文明的專屬卡兵種集中（法蘭西全是騎兵、英國全是遠程），三角會連帶造成文明互剋。
這是**刻意保留**的設計：玩家因此需要用中立卡補強自己被剋的環節，讓組牌真正產生決策。

> 註：AI 模擬用的是固定牌組、不會針對對手調整，所以模擬出的文明勝率差距
> **高估**了真人對戰的失衡程度 —— 真人可以換牌，AI 不行。



---

## 6. 卡表（42 張）

### 中立（14）
民兵 1金2/2🗡 · 木柵欄 1金0/6🏰 · 徵召令 1金(抽2) · 長矛兵 2金2/5防1🗡 · 弓箭手 2金2/2(射2)🏹 ·
斥候 2金1/3(速2)🐐 · 治療祈禱 2金(回3) · 戰號 2金(本回合攻擊+2) · 劍士 3金4/4🗡 · 弩兵 3金3/3(射2)🏹 ·
遊俠騎手 3金3/4(速2)🐐 · 箭雨 3金(目標與同路相鄰各2傷) · 重裝長矛兵 4金3/7防1🗡 · 攻城槌 4金1/8(攻城+4)🔨

| 文明 | 卡牌（各 7 張） |
|---|---|
| 英 | 長弓手 3金3/5(射2)🏹 · 重裝士兵 3金4/5🗡 · 大盾弩手 4金4/6防1(射2)🏹 · 皇家衛隊 5金5/6防1🗡 · 精銳長弓手 5金4/5(射3)🏹 · 火油箭 2金(4傷) · 破甲箭 2金(3傷；目標有防禦力則5) |
| 蒙 | 輕騎兵 2金4/6(速2機動)🐐 · 蒙古弓騎 3金3/5(速2射2機動)🏹 · 草原槍騎 4金5/6(速2機動)🐐 · 遊騎射手 4金5/6(速2射2機動)🏹 · 怯薛軍 6金6/8防1(速2機動)🐐 · 蒙古突襲 2金 · 佯退 2金(恢復移動額度) |
| 法 | 聖騎士 3金3/4防1陣型🐐 · 法蘭西長槍兵 3金3/5防1陣型🗡 · 重裝騎士 4金4/5防1陣型🐐 · 聖殿騎士 4金3/5防1聖盾🐐 · 十字軍 5金4/5防1聖盾陣型🐐 · 王家騎士 6金5/7防1聖盾陣型🐐 · 聖女祝福 4金(回滿血+聖盾) |
| 條 | 路障 2金0/10防1荊棘2🏰 · 條頓長槍陣 3金3/6防1🗡 · 箭塔 4金2/7防1(射2)🏰 · 條頓騎士 4金5/6防1🗡 · 石牆 5金0/10防1荊棘2🏰 · 弩砲 6金3/6防1(射3)🏰 · 修繕 3金(建築回6) |

### 稀有度與卡包

| 稀有度 | 張數 | 抽取權重 | 重複轉換 |
|---|---|---|---|
| 普通 | 22 | 70% | 💎10 |
| 精良 | 16 | 25% | 💎25 |
| 英雄 | 4（每文明各一） | 5% | 💎60 |

- **保底**：每包至少 1 張精良以上。沒有保底的話連開幾包都是普通卡，開包就沒有期待感。
- **重複轉寶石**：超過同名上限的卡自動換成寶石 —— 否則多抽的卡完全沒有用途，這是先前經濟空轉的殘餘問題。

> 權重是「每張卡」的，不是「每個稀有度」的。因為英雄卡只有 4 張，
> 實際開出英雄的比例約 1%，比權重數字低很多 —— 測試斷言的是比例順序與上限，不是權重本身。

### 開包儀式
三幕：**火漆封印 → 打破後羊皮紙展開 → 卡片逐張翻面**，全部用純 CSS。

| 效果 | 做法 |
|---|---|
| 火漆的厚度與壓印 | `radial-gradient` + `inset box-shadow` |
| 封印裂開 | 切成兩片 `clip-path`，各自 `translate + rotate` 飛散墜落 |
| 羊皮紙展開 | `scaleY` 0.06 → 1，`transform-origin:center` |
| 煙塵 | 兩個模糊放射漸層圓擴散淡出 —— 比粒子系統便宜太多且夠用 |
| 翻牌 | `rotateY(180deg)` + `backface-visibility:hidden` + `perspective` |
| 英雄級光效 | 旋轉的 `conic-gradient` 外框 + 畫面震動 |
| 音效 | **Web Audio 合成，不需要任何音檔**：白噪音爆點經低通濾波＝蠟裂聲；上行三音＝英雄級號角 |

### 情緒曲線：懸念要放在翻開「之前」
第一版的三張卡背長得一模一樣，稀有度只在翻開的那 0.5 秒揭曉 —— 情緒來不及堆疊，
而且隨機順序下英雄卡可能第一張就翻掉，後面兩張變成反高潮。

```
改前：破裂(高) → 展開(中) → 發牌(低) → 翻 翻 翻(平平平)
改後：破裂(中) → 發牌(中，帶暗示) → 翻(中) → 翻(中高) → 翻(高潮)
```

三項修正：

| 改動 | 做法 |
|---|---|
| **卡背稀有度預告** | 普通不發光、精良藍光、英雄金光且脈動更快。懸念從 0.5 秒拉長到整段等待 |
| **最好的留到最後** | 依稀有度由低到高排序後才發牌 |
| **落定過衝** | 翻面結束時 `scale(1.07) → 1` 輕彈一下；跳過時不播，避免連續彈跳很吵 |

> **為什麼精良也發光**：原本打算「只有英雄級發光」以保留驚喜，但實測每包最高稀有度
> **94.4% 是精良、只有 5.6% 是英雄** —— 若只有英雄發光，19 包裡有 18 包完全看不到這個效果。
> 改成精良也發光後，保底保證每包至少一張精良，所以**每包都看得到光**，而金色成為罕見的大獎訊號。
> 這是先量數據再決定設計的例子：直覺上「少劇透比較好」，數據卻說那樣等於沒做。

### 華麗化：把資源集中在「英雄級時刻」
爐石的華麗有一大半來自**美術資源**（手繪卡圖、3D 卡包、粒子貼圖），那不是 CSS 補得起來的。
但另一半來自**光、動態、深度** —— 這三樣恰好是 CSS 擅長的，所以往那邊壓。

而且爐石 95% 的卡包其實會被忘記，大家記得並分享的只有傳說那一刻。
我們的英雄級出現率 5.6%，結構相同 —— 與其平均美化，不如讓那 5.6% 變成事件。

| 時機 | 效果 |
|---|---|
| **破封印** | 全畫面白閃（0.34s）→ 光爆圈擴散 → 26 顆火花四射（隨機角度／距離／大小／延遲） |
| **發牌** | 卡片從中心**弧線飛出**到定位，帶旋轉與輕微過衝 |
| **平時** | 卡片跟隨滑鼠 **3D 傾斜**（`rotateX/rotateY`）、hover 抬起、光澤掃過卡面 |
| **翻普通／精良** | 小規模火花（5／10 顆） |
| **翻英雄級** | 其他卡淡出變暗 → 主角**放大 1.55 倍置中** → 背後**旋轉光柱** → 34+20 顆金色火花 → 畫面震動加重、色調轉暖 → 停留 2.3 秒 |
| **卡背** | 從 emoji 換成 CSS 繪製的紋章：放射 conic 紋 + 45° 織紋 + 同心圓浮雕 |

**光柱的做法**：`repeating-conic-gradient` 畫出等角楔形，再用 `mask-image` 的放射漸層讓中心與邊緣淡出，
最後 `rotate` 慢速旋轉。整個效果約 10 行 CSS，不需要任何圖檔。

> **效能原則：動畫只碰 `transform` 與 `opacity`。** 這兩個屬性由合成器處理，不觸發重排重繪；
> 動 `left/top/width` 的話粒子一多就會明顯掉幀。`tests/ui.test.js` 有一條測試會掃描
> 特效區塊裡每一組 `@keyframes`，發現 `left/top/width/height/margin/padding` 就失敗。

**可跳過是必要功能，不是附加。** 開包會重複幾十次，不能跳的動畫到第十包就是折磨。
右上角隨時可跳過，結果頁還能勾「以後直接看結果」，偏好存在存檔裡。

> 動畫本身不是瓶頸 —— **沒有稀有度的話，翻牌沒有懸念**。
> 三張等價的卡翻不翻都一樣，再華麗的動畫也撐不起沒有落差的內容。
> 所以稀有度先做，動畫後做。

### 收藏與卡包經濟
- **起始收藏**：基本中立 8 種 ×2 ＋ 每個文明 2 張招牌卡 ×2 ＝ **剛好能組出一副 20 張的牌組，但沒有任何選擇餘地**。
- 其餘 26 張要靠卡包解鎖。每個文明可用卡種 21 種、收藏上限 42 張，而牌組只能放 20 張 —— **一半的牌要被刷掉**，組牌才真正有取捨。
- 存檔鍵為 `stl_save_v3`；卡池或存檔結構有變時提高版號，舊存檔自動失效重發。

> **為什麼要擴充**：原本卡池只有 22 張、每文明 11 種可用卡，收藏上限 22 張而牌組只需 20 張 ——
> 開局第一分鐘就擁有完整最優牌組，之後抽再多卡包都只是往用不到的倉庫堆東西。
> 整個「寶石 → 卡包 → 擴充牌組」的迴圈是空轉的，組牌階段也感覺不到策略，
> 因為**玩家沒有在做取捨，只是把能放的都放進去**。

### 平衡現況與三個踩過的坑

> AI 互打勝率約 **47% / 43% / 50% / 60%**（英/蒙/法/條），先手 55%，平均 17.5 回合，**0 場未分勝負**。
> 條頓偏高：建築群的守勢在「固定牌組」的 AI 對戰中特別有效。真人對戰差距會小很多，
> 因為玩家可以針對性放攻城槌與弓兵，AI 不會。

**坑一：AI 組牌器讓整輪平衡數據作廢。** 擴充卡池後，怎麼調法蘭西的數值它都卡在 65%。
拿掉它**全部的聖盾**、拿掉**全部的陣型**，勝率各只變動 0.5pp —— 這個「改了等於沒改」的訊號
才讓我去看 AI 實際在用什麼牌組：

```
不列顛  文明卡 4/20 | 民兵×2 木柵欄×2 長矛兵×2 弓箭手×2 斥候×2 劍士×2 弩兵×2 遊俠騎手×2 長弓手×2 重裝士兵×2
法蘭西  文明卡 4/20 | 民兵×2 木柵欄×2 長矛兵×2 弓箭手×2 斥候×2 劍士×2 弩兵×2 遊俠騎手×2 聖騎士×2 法蘭西長槍兵×2
```

組牌器只按費用由低到高塞卡，卡池一擴充，便宜的中立卡就把專屬卡全擠掉 —— **四副牌有 16 張完全相同**。
改成「專屬卡全放 14 張 ＋ 中立補滿 6 張」後，數據才開始對卡牌改動有反應。
**教訓：數值怎麼調都沒反應時，先懷疑量測工具，不要繼續加大改動幅度。**

**坑二：加固城防是隱形的勝利按鈕。** 條頓一度 79%。逐項關閉測試顯示，光是關掉這個技能就讓它掉到
42.8% —— **一個技能造成 33pp**。它每回合花 2 金替城堡回 3 血，而疲勞是遞增的消耗戰，等於白送一場。
改成「建築回 5、城堡只回 2」後，價值移到建築身上（更符合條頓定位），也不再左右消耗戰。

**坑三：先手優勢。** AI 牌組變得文明特色濃厚（費用曲線變高）後，先手勝率一度到 **61%**。
加入後手補償（第一回合多 2 金，類似爐石的硬幣）後降到約 **55%**。

## 6.5 操作與體驗

| 功能 | 說明 |
|---|---|
| **起手換牌** | 開局可指定要換掉的起手牌。**先補抽、再把換掉的洗回牌庫** —— 順序寫反會抽回自己剛丟掉的那張，換牌就白換了。NPC 也會換掉 5 費以上的起手 |
| **復原（↶ / Ctrl+Z）** | 以整份 G 的深拷貝當快照。只能復原到本回合開始 —— 跨回合復原等於看過 AI 行動再反悔 |
| **Esc** | 取消選取單位、取消出牌、取消英雄技能的指定模式 |
| **投降確認** | 離開對戰前跳確認，避免手滑 |
| **滑鼠提示** | 棋子上顯示完整數值與**關鍵字白話說明**，不必背；對手文明的特色與英雄技能也可查看 |
| **移動動畫** | 棋子從舊格子滑到新格子（0.18s），看得出發生了什麼 |
| **相剋表** | 常駐在盤面左側的空白區（絕對定位，盤面維持置中）。**選取單位時會高亮「它剋誰」與「誰剋它」**，從靜態說明變成當下的決策輔助。內容由 `COUNTER_TABLE` 推導，改規則會自動跟上。視窗小於 1042px 自動收合成小標籤，點一下可展開 |
| **攻擊特效** | 遠程射出箭矢飛向目標；近戰的刀光**從右上一個點沿 135° 往左下延伸成線**再淡出（`scaleX` 0→1，支點固定在起點），像揮砍劃出的軌跡。命中跳出 `-N` 傷害數字，血量歸零後殘影淡出。攻擊城堡同樣有特效 |

### 特效為什麼要用佇列
戰鬥邏輯是同步的（AI 與測試都依賴這點），動畫不能卡在中間。
所以邏輯執行時只把「要播什麼」排進 `fxQueue`，`render()` 之後才一次播放：

```
attack()  → 排入 箭矢/刀光，回傳命中時刻
damage()  → 排入 -N 傷害數字（對齊命中時刻）；血量歸零再排入陣亡殘影
render()  → 重建盤面 → playFx() 依各自的 animation-delay 播放
```

殘影的存在是為了讓「數字跳出 → 血量歸零 → 單位消失」看得出先後 ——
`cleanup()` 其實在畫面更新前就把單位移除了，殘影補上那段空白。

這順帶修掉一個一直沒作用的舊功能：原本的 `flash()` 在 `render()` 重建盤面後
class 就沒了，棋子受擊閃爍**從來沒有真正播放過**。

### 踩過的坑：特異性又一次（刀光看不見）
特效層原本用 `#fx > *{opacity:0}` 讓元素在 `animation-delay` 期間先隱藏。
那條選擇器帶 id，特異性 1-0-0，蓋過 `.fx-slash{opacity:1}` 的 0-1-0。

箭矢與傷害數字沒事，因為它們自己的動畫在播放時會覆蓋 `opacity`；
但刀光的**容器**沒有動畫（動畫在子元素刀身上），於是全程停在 opacity 0 ——
刀身再怎麼正確也看不見。

正確做法是 `animation-fill-mode: both`：延遲期間元素停在 0% 的狀態，不需要外加 opacity 規則。

當時的測試檢查了「元素存在、transform 是 scaleX(0)、支點在起點」，全部通過 ——
**唯獨沒有檢查它看不看得見**。現在補了一條測試，會沿著 DOM 往上逐層檢查
每個特效元素的 `opacity`，只要有一層是 0 且沒有動畫可覆蓋就判定失敗。

**教訓：驗證視覺效果時，「存在」和「可見」是兩件事。**

### 相剋表放哪裡
盤面改成縱向後只剩 264px 寬，兩側是空的 —— 1024px 視窗仍各有 255px。
相剋表就放在左側空白區，理由：

- **不必點開**：相剋是每次攻擊都要判斷的事，藏在按鈕後面等於沒有
- **不佔用有效空間**：填的是版面本來就浪費掉的區域
- **不推移盤面**：用絕對定位，盤面維持在畫面正中央

臨界寬度 1042px 是算出來的：面板右緣 236px ＋ 20px 間距，兩側對稱，
再加上 264px 盤面與 250px 戰報欄。低於這個寬度就自動收合，並監聽 resize。

## 7. NPC 行為

NPC 走的是**和玩家完全相同的規則**（同樣的行動額度、召喚失調、反擊），沒有作弊：
1. 村民抉擇：前 3 回合或壓力小時優先發育，否則挑敵人少、領土淺的路拓荒
2. 出牌：優先補防守最空虛的路；遠程單位放後排以保住射程，近戰放最前線
3. 全軍行動：由最前線往後，每個單位「射程內有敵人就打 → 否則盡量往前走 → 走完再試著攻擊」

足夠當練習對手。想加強的話，最直接的下一步是把第 3 步換成「模擬各種行動組合後評分」的搜尋式 AI。

## 8. 原型範圍與後續

**已實作**：完整對戰（玩家手動操控移動與攻擊）、起手換牌、復原、行動額度／召喚失調／反擊／打帶跑、四文明與英雄技能、領土與村民抉擇、牌組編輯、卡包與寶石、存檔（localStorage）、NPC。
**後續可加**：卡牌稀有度與塵、每日任務、天梯、更多建築與法術、動畫與音效、連線對戰。

## 9. 檔案結構

**資料與邏輯分離** —— 之後新增卡片或文明只需要動 `data/` 底下的檔案，不必碰引擎。

```
index.html          版面骨架（四個 .screen：主選單／組牌／商店／戰場）
style.css           全部樣式
data/rules.js       規則常數：路數、格數、城堡血量、金幣上限、相剋表、存檔版號、卡包售價
data/cards.js       卡牌資料（42 張）＋ 中立清單 ＋ 起始收藏設定
data/civs.js        文明資料（名稱、英雄、特色、技能）
game.js             引擎：戰鬥、AI、UI、存檔
tools/loadgame.js   共用載入器：把四個檔重現成 Node 可執行的環境
tools/sim.js        AI 對戰平衡模擬器
tools/check.js      一鍵回歸
tests/              120 項測試（規則／法術／介面／端對端／部署載入）
```

載入順序在 `index.html` 中固定為 `rules → cards → civs → game`（`defer`，依序執行）。
四個檔案共用同一個全域作用域，不使用模組，維持「純 HTML、無框架、無建置步驟」。

### 新增一張卡
只要在 `data/cards.js` 加一筆。欄位說明寫在該檔開頭，例如：
```js
halberd:{kind:'I',n:'戟兵',cost:4,atk:4,hp:6,def:1,rng:1,spd:1,tags:['步兵'],civ:'N',t:'說明文字'},
```
若是中立卡，記得同時加進 `NEUTRAL`；要讓它成為起始牌才需要加進 `STARTER_N`。

### 新增一個文明
1. `data/civs.js` 加一筆 `CIVS` 條目（含 `sk` / `skCost` / `skT`）
2. `data/cards.js` 加上 `civ:'<代號>'` 的卡牌
3. `data/cards.js` 的 `STARTER_CIV` 指定兩張起始招牌卡
4. `game.js` 的 `useHero()` 與 `aiTurn()` 實作該英雄技能

前三步只要有缺漏或打錯字，開機時的 `validateData()` 會直接把問題列在畫面上：
```
資料檢查發現 3 個問題：
卡牌 sword：kind 'X' 無效（可用：I/C/R/S/B）
卡牌 crossbow：缺少數值 atk
文明 france 的起始卡 longbow 其實屬於 brit
```
它也會檢查「起始收藏是否剛好湊得滿 20 張牌組」，避免新增文明時忘了配起始卡。

### 開發流程
```bash
npm install       # 只為了測試用的 jsdom，遊戲本身不需要任何相依
npm run check     # 一鍵回歸：語法 → 資料驗證 → 51 項測試 → 平衡模擬
npm run sim -- 200 --trace   # 加大樣本並統計相剋觸發率
npm run decks     # 印出各文明的 AI 牌組（用來確認牌組真的有文明特色）
```

**改動任何規則或數值後請跑 `npm run check`。**

測試刻意涵蓋幾類「人眼容易漏、jsdom 抓得到」的問題，都是實際踩過才補的：
- 畫面切換要斷言 `getComputedStyle(el).display`，不能只斷言 class
- 卡面的絕對定位元素兩兩比對 `top/right/bottom/left`，定位相同就必定重疊
- 單位的每一種狀態組合都要有對應的畫面標示
- 「打不到的敵人」必須給出原因，不能靜默無反應

驗證測試本身有效的方式：故意把 `COUNTER_BONUS` 改成 0、讓召喚失調失效、把卡牌的
`kind` 打錯字 —— 三種破壞都被準確攔下並指出位置。

### 相剋改為查表
`data/rules.js` 的 `COUNTER_TABLE` 定義誰剋誰，引擎只做查表。
要新增兵種或調整相剋關係，改這張表即可，不必動 `game.js`。

### 踩過的坑：CSS 特異性讓畫面切換失效
```css
.screen{display:none}   /* (0,1,0)，定義在前 */
.col{display:flex}      /* (0,1,0)，定義在後 → 贏 */
```
`#deckbuild/#shop/#battle` 的 class 都是 `"screen col"`，兩條規則特異性相同、後者勝出，
於是這三個畫面**永遠隱藏不掉**，各自佔 100vh 垂直堆疊，`body{overflow:hidden}` 只讓人看到最上面那個。
JS 完全正確（`classList` 該加該移都對），畫面卻不會變 —— 而且 jsdom 不做版面堆疊，測 `classList.contains('active')` 一路通過。
修法是把特異性提到 (0,2,0)：`.screen:not(.active){display:none}`。
**教訓：驗證畫面切換要斷言 `getComputedStyle(el).display`，不能只斷言 class。**

### 踩過的坑：狀態沒有完整對應到畫面（同一個坑踩了兩次）
**第二次**：英雄技能「齊射命令」讓遠程單位攻擊 +1，實測傷害確實從 1 變 2 —— 但棋子上顯示的攻擊力
仍是原始的 `u.atk`，玩家完全看不出技能有沒有生效，自然會以為技能壞了。
修法是顯示 `buffAtk(u)` 並在有增益時高亮，另外用獨立的 HUD 欄位標示「齊射生效中」。
（第一版我把增益訊息寫進 `hint`，結果蓋掉了「本回合已攻擊過」之類的操作說明 —— 
改用獨立欄位才不會互相搶。）

**第一次**：
行動額度標記原本寫成 `u.sick?'💤' : (!canAct(u)?'✓' : (u.moved?'·':''))`，
漏掉了「**已攻擊但還沒移動**」這個狀態 —— `canAct` 仍為 true、`u.moved` 是 false，三個分支全部落空，
棋子看起來跟全新的一樣（還亮著可行動外框、也還有綠色移動格），點敵人卻毫無反應。
邏輯完全正確，但玩家無從得知原因。改成直接標示「還剩什麼行動」，並在攻擊失敗時說明理由
（已攻擊過／跨路／超出射程）。
**教訓：只要單位有 N 個獨立的狀態位元，畫面就必須把 N 種組合都表達出來。**

### 踩過的坑：捲動容器會裁切自己的子元素
手牌列選中的卡片會往上抬 14px 表示「已選取」，但看起來像被上方的操作列蓋住。
實際原因不是圖層，是 `.hand` 自己把它裁掉了：

```css
.hand{ padding:8px 16px; overflow-x:auto }   /* 上方只有 8px */
.card.sel{ transform:translateY(-14px) }      /* 卡片抬起 14px */
```

CSS 規範中，`overflow-x` 只要不是 `visible`，`overflow-y` 就會從 `visible` 變成 `auto` ——
橫向捲動的容器同時也會**縱向裁切**。留白 8px 裝不下抬起的 14px，超出的部分連同光暈一起被切掉。

修法是把上方留白加到 22px。`tests/ui.test.js` 有一條測試守住這個關係：
讀出 `.card.sel` 的 `translateY` 與 `.hand` 的 `padding-top` 做比較，
之後有人調整抬起高度而忘了改留白，會立刻失敗。

**教訓：`overflow-x:auto` 不只影響水平方向。**

### 踩過的坑：診斷工具把整個遊戲弄死
使用者的朋友打開線上版，組牌頁一片空白 —— 文明列表、卡池全空，寶石顯示 0，
而且**沒有任何錯誤訊息**。線上的四個 JS 檔經確認都是最新且完整的。

關鍵線索是右下角的「點擊探針」**沒有被隱藏**（它在 `boot()` 的最後一步才會隱藏），
而畫面上的 `0 / 20`、`💎 0` 其實是 `index.html` 的靜態預設值 —— 代表 JS 從未更新它們。

原因在頂層程式碼的順序：

```js
else boot();                        // ← boot() 在這裡執行
window.addEventListener('error',…)  // ← 錯誤處理器「之後」才註冊
let save = load();                  // ← 存檔「之後」才建立
```

`boot()` 裡全是診斷與輔助功能（資料驗證、快捷鍵、id 自檢、點擊探針），
但它一旦拋錯，**後面的頂層程式碼全部不執行** —— `save` 沒建立，遊戲直接死掉；
而且錯誤處理器還沒註冊，所以連錯誤訊息都沒有。

修正三件事：
1. 錯誤處理器移到**檔案最前面**，在任何可能拋錯的程式之前
2. `boot()` 包 try/catch —— 診斷工具壞掉不該讓遊戲跟著死
3. `boot()` 的呼叫移到**檔案最末端**，此時所有 `let/const` 都已初始化（消除整類 TDZ 風險）

**為什麼 107 項測試全綠卻沒抓到**：所有測試都把四個 JS 內嵌成單一 `<script>` 執行，
但線上是四個外部 `<script defer>`。兩者的 `document.readyState` 不同，
`boot()` 因此走了不同分支。新增 `tests/deploy.test.js` 用真實的外部載入路徑重跑，
並斷言「boot() 失敗時遊戲仍能初始化」。

**教訓：測試環境與部署環境的載入方式必須一致，否則測的不是同一個東西。**

### 除錯選單（F2）
原型專用的測試輔助，任何畫面都能用 F2 開關。刻意用橘紅色框，一眼看得出不是正式介面。

| 分類 | 功能 |
|---|---|
| 存檔 | 寶石 99999 / 0、收藏設為「初始」或「全滿」、清空存檔（會先確認） |
| 對戰中 | 我方城堡回滿、敵方城堡剩 1、金幣拉滿、抽 3 張、三路領土 +1、清空盤面 |

面板頂端顯示目前的寶石、收藏種類與張數、雙方城堡血量；對戰類按鈕不在對戰中會自動停用。

**正式版移除方式**：刪掉 `index.html` 的 `#debugPanel`、`style.css` 的 `#debugPanel` 區塊，
以及 `game.js` 的「除錯選單」整段即可，其餘程式碼沒有依賴它。

> 順帶補了一個相關的健壯性問題：把收藏改小之後，已存的牌組可能含有「已經不擁有的卡」，
> 卻還能帶著違規牌組進場。現在 `openDeckbuild()` 會用 `sanitizeDeck()` 依目前收藏與文明過濾牌組。

### 踩過的坑：jsdom 不展開 CSS 簡寫
測試用 `getComputedStyle(el).animationName` 判斷元素有沒有動畫，結果**永遠是 `"none"`** ——
jsdom 的 cssstyle 不會把 `animation: name 1.9s ease-in-out infinite` 這種簡寫展開成
`animationName`，而是原封不動留在 `animation` 裡。

後果是「特效元素是否看得見」那條測試裡的 `hasAnim` 判斷一直失效（雖然它仍抓到了原本的 bug）。
`tests/ui.test.js` 現在有共用的 `animName(style)` 會從簡寫字串取出名稱。

**教訓：斷言 computed style 前，先確認測試環境真的算得出那個屬性。**

### 踩過的坑：測試沒走到它要測的路徑
「跳過時完全不播特效」這條測試，在**把清除邏輯整段刪掉之後依然通過**。
原因是測試直接 `buyPack()` → `packSkip()`，中間沒有破封印，特效層本來就是空的 ——
它斷言的是一個恆真的條件。

改成先 `packBreak()` 讓特效層真的有 27 個元素，再跳過並斷言清空，刪掉邏輯就會紅。

**教訓：每條測試都要確認「拿掉被測的程式碼之後它會失敗」，否則那只是一行昂貴的註解。**
這個專案的做法是每次加完測試就故意破壞一次來驗證。

### 除錯機制
畫面上有兩個診斷元件（原型專用，正式版可移除）：
- **開機警告條**：`game.js` 成功執行才會移除。若它一直在，就是資源沒載到。
- **🐞 點擊探針**（右下角，網址加 `?debug` 才顯示）：顯示滑鼠位置最上層的元素、按鈕是否被覆蓋或 disabled，以及 `startBattle()` 的逐步追蹤。


`game.js` 註冊了 `window.onerror` / `unhandledrejection`，任何 JS 例外都會用紅色浮層顯示在畫面最上方，
不會再出現「點了沒反應」卻查不到原因的情況。`localStorage` 也全部包了 try/catch
（以 `file://` 開啟或無痕模式時會丟 SecurityError），寫入失敗只會提示「進度不會保留」，不會中斷遊戲。
